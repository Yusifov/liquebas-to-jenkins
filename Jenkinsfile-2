pipeline {
  agent {
    docker { image 'liquibase/liquibase:4.4.2' }
  }
  // environment {
  //   MARIADB_CREDS=credentials('mariadb-credentials')
  // }
  stages {
    stage('Status') {
      steps {
        sh 'liquibase status --url="jdbc:postgresql://experianconvexdev.galaksiya.net/pgadmin;databaseName=mlflow" --changeLogFile=my_app-wrapper.xml --username=postgres --password=fQy9yHzy'
      }
    }
    // stage('Update') {
    //   steps {
    //     sh 'liquibase update --url="jdbc:postgresql://postgresql.postgresql.svc:5432/mlflow" --changeLogFile=my_app-wrapper.xml --username=postgres --password=fQy9yHzy'
    //   }
    // }
  }
  post {
    always {
      cleanWs()
    }
  }
}